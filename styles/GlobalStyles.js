import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  containerNav: {
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
  },
  titleNav: {
    fontSize: 18,
    marginLeft: 10,
  },
});
